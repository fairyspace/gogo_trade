# 资金系统设计（权威版）

> 本文是资金域（Fund Service）的权威设计。对标 MetaQuotes / Spotware / Devexperts / Apex / DriveWealth / TT / Broadridge / Talos / 恒生电子等全球经纪与清算科技实践，并结合本平台已落地的账户体系、交易引擎契约与代理体系契约。  
> 原稿中出入金状态机、通道抽象、冻结明细、风控与对账思路予以吸收；**余额权威、原路退回粒度、幂等、拒付、客户资金隔离、事件契约以本文为准**。方向设计描述职责分层，**不锁定实现语言**。

---

## 一、系统定位与对标决策

### 1.1 系统定位

Fund Service 是经纪平台的**支付编排、客户资金生命周期、出款执行、公司账簿分录与外部对账中心**，支持：

- 法币入金 / 出金（PSP、银行电汇、电子钱包、本地支付）
- 同用户多交易账户内部转账（含跨币种换汇）
- 出金冻结、风控审核、原路退回（AML）
- 拒付（Chargeback）回拨与客户负债处理
- Partner 钱包出款执行（佣金账本仍属代理域）
- 客户 ledger 与公司双分录 journal 的日切对账

以下概念**不属于**本系统：

- 交易盈亏、佣金、Swap、分红的计算（交易/结算域写入 ledger）
- KYC/AML 身份审核本身（准入域；本系统只消费结果）
- 代理佣金计提与 Partner 余额账本（代理域）
- 撮合引擎内存保证金实时计算（引擎域；本系统只调用预检/资金 RPC）
- 美股清算托管、期货 SPAN、加密 Prime 全栈

### 1.2 行业参考的正确使用方式

| # | 厂商 | 可借鉴能力 | 本设计采纳 / 取舍 |
|---|------|-----------|------------------|
| 1 | **MetaQuotes** | Deal/Ledger 联动；Credit 不可提；Manager 资金操作接口形态 | 采纳 Credit 不可提、ledger 类型与交易联动；**不把 Manager 当完整支付/合规系统** |
| 2 | **Spotware (cTrader)** | 身份与多账户；内部转账 | 采纳同用户多账户转账；**不做多币种 Asset 钱包**（与账户体系决策一致） |
| 3 | **Devexperts** | 风险维度可提校验（含保证金水平） | 出金前 `PreviewWithdraw`；不做完整组合保证金账本 |
| 4 | **Apex Fintech** | API-first 出入金状态；拒付/回拨；账户类型约束 | 采纳 Chargeback、状态机、API 幂等 |
| 5 | **DriveWealth** | 资金指令编排、账实分离 | 采纳 Payout Instruction 统一出款；不做美股清算托管全栈 |
| 6 | **TradingView** | — | 不在资金域 |
| 7 | **Trading Technologies** | 账户级出金前风控阈值 | 阈值下沉到账户组/资金风控规则；不做 SPAN |
| 8 | **Broadridge** | Books & Records；职责分离；多实体账簿 | 采纳公司双分录 journal、`entity_id` 隔离、四眼原则 |
| 9 | **Talos** | 机构子账户资金边界意识 | 边界意识保留；不做加密 Prime 全栈 |
| 10 | **恒生电子** | 客户资金隔离；对账；三方存管思想 | 采纳客户资金负债科目与日切对账；**不照搬证券三方存管拆户**（CFD 单账户即资金+持仓主体） |

### 1.3 产品定位（拍板）

零售 / 专业客户多资产 CFD/FX 经纪平台资金域：

- **法币优先（P0）**，加密链上为 **P2**
- 单账户单结算币种；跨币种 = 同用户多账户 + 内部转账换汇
- 与《账户体系设计》《代理系统设计》《交易系统设计》引擎资金契约兼容

---

## 二、十二条不可破坏决策

1. **余额唯一变更路径**：任何改变交易账户 `balance / credit / frozen_balance` 的操作，必须经交易引擎 RPC：`CreditAccount / DebitAccount / FreezeFunds / UnfreezeFunds / AdjustCredit / TransferBetweenAccounts`。**Fund 编排层禁止直写** `t_trading_account` 资金字段。
2. **客户账本权威是 `t_account_ledger`**：链式 `balance_before → balance_after` 为客户侧真相；账户表资金字段仅为引擎同步快照。
3. **券商账簿双分录独立**：`t_journal_entry` + `t_journal_line` 记录客户资金负债、PSP 应收应付、手续费收入、汇兑损益；**不替代**客户 ledger。
4. **单账户单币种不变**：跨币种需求由内部转账 + 汇率快照满足。
5. **`entity_id` 贯穿**：所有资金聚合与流水带实体；禁止跨实体转账 / 出款 / 调账。
6. **幂等优先**：外部回调、引擎资金 RPC、审核动作均带 `idempotency_key`；重复请求返回首次结果。
7. **冻结先于扣款**：出金 / 争议先 `FreezeFunds`，成功再 `DebitAccount` 并释放冻结；拒绝则 `UnfreezeFunds`。禁止“先扣后补”。
8. **原路退回按入金批次 FIFO**：权威在 `t_deposit_lot` + `t_deposit_lot_consumption`；盈利部分才允许非原路通道；拒付优先消耗对应批次。
9. **Credit 永不计入可提**：可提金额以引擎实时态为准（含浮动盈亏与保证金缓冲）。
10. **事件经 Outbox**：至少发布 `deposit.completed.v1`、`deposit.chargeback.v1`、`withdrawal.completed.v1`、`withdrawal.failed.v1`、`transfer.completed.v1`、`partner.payout.completed.v1`、`partner.payout.failed.v1`。
11. **四眼原则**：人工审核通过、人工调账、大额出金满足 SoD（权限系统 RBAC）；申请人 ≠ 审批人。
12. **冲正只追加不改历史**：拒付、失败回滚、纠错一律新增 ledger / journal 反向分录，禁止改历史金额。

---

## 三、边界与跨系统契约

### 3.1 领域边界

| 系统 | 权威数据 | Fund Service 的使用方式 |
|------|---------|------------------------|
| Onboarding / 准入 | KYC 等级、AML 状态、协议签署 | 消费结果驱动限额与通道可见性 |
| User | `user_id`、`entity_id` | 引用主体；不复制身份资料 |
| Account | 交易账户、账户组、`can_withdraw`、结算币种 | 读状态与实体；写余额只经引擎 |
| Trading Engine | 实时 balance/equity/margin、ledger 写入 | 资金 RPC + PreviewWithdraw |
| Partner | 佣金账本、结算单、Partner 可用余额 | Fund 仅执行 `PARTNER_WALLET` 出款 |
| Notification | 模板与送达 | Fund 只发标准业务事件 |
| PMS / 权限 | RBAC/ABAC/SoD | 审核与调账权限校验 |

### 3.2 引擎资金 RPC 契约（供 Fund 编排层调用）

所有写接口必须携带 `request_id`（幂等键）。重复 `request_id` 返回首次成功结果，不二次变更余额。

| RPC | 语义 | 典型调用方 |
|-----|------|-----------|
| `CreditAccount` | 增加 balance（或指定 ledger_type），写 ledger，刷新内存 | 入金入账、调账入、内部转入、模拟注资 |
| `DebitAccount` | 扣减 balance（需满足可扣约束），写 ledger | 出金完成、拒付扣回、调账出、内部转出 |
| `FreezeFunds` | 增加 frozen，不改 balance；写冻结明细关联 | 出金创建、争议冻结 |
| `UnfreezeFunds` | 释放冻结 | 出金拒绝/失败/取消 |
| `AdjustCredit` | 发放/回收 credit（不可提） | 运营赠金 |
| `PreviewWithdraw` | 只读：返回 withdrawable 与模拟扣款后 margin_level | 出金前置校验 |
| `TransferBetweenAccounts` | 同请求号原子转出+转入（可含汇率后金额） | 内部转账 |
| `GetAccountState` | 只读实时态 | 查询可提、运营看板 |

```protobuf
// 示意：engine/fund.proto
message CreditAccountRequest {
  int64  account_id     = 1;
  string amount         = 2;  // 十进制字符串，法币 4 位小数
  string currency       = 3;
  uint32 ledger_type    = 4;
  int32  direction      = 5;  // 恒为 +1
  string reference_no   = 6;
  int64  deposit_id     = 7;
  string request_id     = 8;  // 幂等键，必填
  int64  entity_id      = 9;
  string remark         = 10;
}

message FundMutationResponse {
  bool   success        = 1;
  string reject_reason  = 2;
  int64  ledger_id      = 3;
  string balance_after  = 4;
  string frozen_after   = 5;
  string equity_after   = 6;
}

message PreviewWithdrawRequest {
  int64  account_id     = 1;
  string amount         = 2;  // 拟提金额；空则只返回上限
  string request_id     = 3;
}

message PreviewWithdrawResponse {
  bool   allowed              = 1;
  string reject_reason        = 2;
  string withdrawable         = 3;
  string balance              = 4;
  string equity               = 5;
  string margin_used          = 6;
  string margin_level_after   = 7;  // 模拟扣款后
  string frozen_balance       = 8;
  string credit               = 9;
}
```

### 3.3 关键事件（Outbox）

所有事件必须包含：`event_id`、`event_type`、`occurred_at`、`entity_id`、`aggregate_id`、`aggregate_version`、`correlation_id`、`payload`。生产方写库与写 Outbox 同事务；禁止“写库成功后尽力发 MQ”。

| 事件 | 生产方 | 主要消费方 |
|------|--------|-----------|
| `deposit.completed.v1` | Fund | Partner（首存 CPA）、Notification、Account 激活 |
| `deposit.chargeback.v1` | Fund | Partner（CPA 回拨）、Risk、Notification |
| `withdrawal.completed.v1` | Fund | Notification、报表 |
| `withdrawal.failed.v1` | Fund | Notification、运营 |
| `transfer.completed.v1` | Fund | Notification |
| `partner.payout.completed.v1` | Fund | Partner |
| `partner.payout.failed.v1` | Fund | Partner |

---

## 四、分层架构

```
┌──────────────────────────────────────────────────────────────────────────┐
│  Clients: App / Web / Ops Admin / PSP Webhook / Chain Monitor(P2)         │
└───────────────────────────────┬──────────────────────────────────────────┘
                                │
┌───────────────────────────────▼──────────────────────────────────────────┐
│  Fund Service（编排层）                                                    │
│  ┌─────────────────┐  ┌──────────────┐  ┌─────────────────────────────┐ │
│  │ Orchestration   │  │ Fund Risk    │  │ Payment Adapters            │ │
│  │ 入金/出金/转账   │──│ 限额/冷静期/  │──│ Stripe/Skrill/Bank/Crypto   │ │
│  │ Chargeback      │  │ 原路/审核路由 │  │ + Payout Instruction 执行    │ │
│  └────────┬────────┘  └──────────────┘  └─────────────────────────────┘ │
│           │  ┌──────────────┐  ┌──────────────┐  ┌───────────────────┐  │
│           ├──│ Idempotency  │  │ Journal      │  │ Outbox Publisher  │  │
│           │  └──────────────┘  │ (公司账簿)    │  └───────────────────┘  │
│           │                    └──────────────┘                          │
│           │  ┌────────────────┐                                          │
│           └──│ Reconciliation │ 日切 PSP/银行/ledger/journal 对账        │
│              └────────────────┘                                          │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │ 资金变更 RPC
┌───────────────────────────▼──────────────────────────────────────────────┐
│  Trading Engine（余额写权威）                                              │
│  Credit/Debit/Freeze/Unfreeze/AdjustCredit/Transfer/PreviewWithdraw       │
│  → 原子更新内存账户 + 写入 t_account_ledger                                │
└───────────────────────────┬──────────────────────────────────────────────┘
                            │
        ┌───────────────────┼───────────────────┐
        ▼                   ▼                   ▼
 t_account_ledger    t_trading_account     Redis 投影
 (客户真相)           (快照)                (只读缓存)
```

**职责切割一句话：**

- Fund 编排层 = 订单状态机 + 支付适配 + 风控编排 + 公司 journal + 对账 + 事件
- Trading Engine = 客户余额与冻结的唯一写者 + 可提预检

---

## 五、金额模型与可提公式

### 5.1 精度

- 法币账户金额：`DECIMAL(18,4)`，与引擎定点数（×10000）对齐
- 汇率：`DECIMAL(18,8)`；加点前后均落快照
- 禁止在业务层使用 IEEE 浮点做资金运算

### 5.2 账户资金语义（与账户体系一致）

| 字段 | 含义 |
|------|------|
| `balance` | 已结算资金（不含浮动盈亏） |
| `credit` | 券商赠送、可交易、**不可提取** |
| `equity` | `balance + credit + floating_pnl` |
| `margin_used` | 已用保证金 |
| `frozen_balance` | 出金/争议等冻结合计（明细见 `t_account_fund_freeze`） |

### 5.3 可提公式（引擎权威）

```
equity       = balance + credit + floating_pnl
available    = equity - margin_used - frozen_balance

# credit 不可提：可提不得超过「非信用已结算资金」
cash_equity  = balance - frozen_balance   # balance 本身不含 credit

withdrawable = min(
  max(cash_equity, 0),
  max(available - margin_call_buffer, 0)
)

# 出金预检：模拟扣款 amount 后
#   margin_level_after = (equity - amount) / margin_used * 100
#   必须仍 >= account_group.margin_call_level（或更严的资金风控阈值）
# 无持仓(margin_used=0)时跳过 margin_level 检查，仅校验 withdrawable
```

出金创建前必须调用 `PreviewWithdraw`；Redis `fund:available:{account_id}` 仅为展示投影，**不得**作为放行依据。

### 5.4 原路退回与盈利

```
某支付方式可退余额 = Σ lot.remaining_amount（同 user + channel + payment_method + currency）
用户总可退本金     = Σ 所有 lot.remaining_amount（同 user，按账户币种折算后）
盈利可自选部分     = max(withdrawable - 总可退本金折算, 0)
```

出金分配规则：

1. 优先按 FIFO 消耗可退 lot（原路通道 + 原支付方式）
2. 超出本金的部分记为 `PROFIT_PORTION`，允许自选已验证通道
3. 监管要求强制原路时，盈利部分仍可限制为白名单通道

---

## 六、核心状态机

### 6.1 入金 `t_deposit_order`

```
CREATED → PENDING_PAY → PAID → CONFIRMING → CREDITING → CREDITED → COMPLETED
                 │         │         │
                 │         │         └→ REJECTED（风控拒绝，走退回/不入账）
                 │         └→ FAILED
                 └→ EXPIRED / CANCELLED
```

| 状态 | 含义 |
|------|------|
| CREATED | 单已建 |
| PENDING_PAY | 等待用户在 PSP/链上完成支付 |
| PAID | 外部已确认收款（金额/币种已核对） |
| CONFIRMING | 资金风控审核中 |
| CREDITING | 已调用引擎入账，等待确认（防超时重试） |
| CREDITED | 引擎入账成功，lot 已生成 |
| COMPLETED | journal/outbox 收尾完成 |
| FAILED / EXPIRED / CANCELLED / REJECTED | 终态 |

### 6.2 出金 `t_withdrawal_order`

```
CREATED → FROZEN → PENDING_REVIEW → APPROVED → PAYOUT_SUBMITTED → COMPLETED
                      │                │              │
                      └→ REJECTED      └→ CANCELLED   └→ FAILED（可重试或转 REJECTED 解冻）
```

| 状态 | 含义 |
|------|------|
| CREATED | 校验通过，待冻结 |
| FROZEN | 引擎冻结成功，lot 消耗预占已写入 |
| PENDING_REVIEW | 待自动/人工审核 |
| APPROVED | 审核通过，待出款 |
| PAYOUT_SUBMITTED | 已提交 PSP/银行/链上 |
| COMPLETED | 外部确认成功 + 引擎 Debit 成功 |
| REJECTED / CANCELLED / FAILED | 终态（失败路径必须 Unfreeze + 释放 lot 预占） |

### 6.3 内部转账 `t_internal_transfer`

```
CREATED → PROCESSING → COMPLETED
                └→ FAILED
```

同事务语义由引擎 `TransferBetweenAccounts` 保证；Fund 侧状态为编排投影。

### 6.4 拒付 `t_chargeback`

```
OPEN → RECOVERING → FUNDS_RECOVERED → CLOSED
                 └→ DEFICIT_CREATED → CLOSED
```

---

## 七、核心业务流程

### 7.1 入金（Deposit）

```
用户发起入金
  → 校验账户状态 / entity / 通道 / KYC / 限额
  → 创建 t_deposit_order (PENDING_PAY) + 幂等键
  → 法币: 调 PSP 获取支付链接
    加密(P2): 分配充值地址
  → PSP/链上回调 (idempotency = provider+psp_order_id)
      → 验签、金额币种核对、汇率快照
      → status=PAID → 风控
          PASS → CREDITING
               → Engine.CreditAccount(request_id=deposit_no)
               → 写 t_deposit_lot
               → 写公司 journal（借: PSP应收/银行  贷: 客户资金负债）
               → Outbox deposit.completed.v1
               → CREDITED → COMPLETED
          HOLD → CONFIRMING → 人工四眼 → 同上或 REJECTED
          REJECT → 不入账，原路退回/标记失败
```

**关键约束：** 仅引擎入账成功后才存在客户可见余额；PAID 但未 CREDITED 不计可交易资金。

### 7.2 出金（Withdrawal）

```
用户发起出金
  → Engine.PreviewWithdraw
  → 原路 FIFO 规划（t_deposit_lot）+ 盈利部分通道校验
  → 冷静期 / 限额 / 密码修改锁 / 账户 can_withdraw
  → 创建订单 CREATED
  → Engine.FreezeFunds(request_id=withdrawal_no:freeze)
  → 写 t_account_fund_freeze + lot consumption(RESERVED)
  → FROZEN → 风控路由
       自动通过 / 人工四眼 → APPROVED
       拒绝 → Unfreeze + 释放 RESERVED → REJECTED
  → 创建 t_payout_instruction → 适配器出款 → PAYOUT_SUBMITTED
  → 外部成功回调
       → Engine.DebitAccount(request_id=withdrawal_no:debit)
       → freeze 状态→DEBITED，lot consumption→CONSUMED
       → journal（借: 客户资金负债  贷: PSP应付/银行）
       → Outbox withdrawal.completed.v1
  → 外部失败
       → Unfreeze + 释放 RESERVED
       → Outbox withdrawal.failed.v1
```

### 7.3 内部转账

```
校验: 同 user OWNER、同 entity、源账户可转、币种/汇率
  → Engine.TransferBetweenAccounts(request_id=transfer_no)
       写 TRANSFER_OUT + TRANSFER_IN 两条 ledger
  → 跨币种: 落 t_exchange_rate_snapshot + journal 汇兑损益
  → Outbox transfer.completed.v1
```

### 7.4 拒付 Chargeback

```
PSP 拒付通知 (幂等)
  → 创建 t_chargeback OPEN
  → 定位原 deposit + lots
  → Engine.DebitAccount 或不足额时部分扣 + 账户限制
  → lot 标记 chargeback 消耗
  → journal 冲回客户负债 / 记损失
  → Outbox deposit.chargeback.v1（供 Partner CPA 回拨）
  → FUNDS_RECOVERED 或 DEFICIT_CREATED → CLOSED
```

---

## 八、表清单

```text
资金体系
│
├── 【通道与收款方式】
│   ├── t_payment_channel              支付通道定义
│   ├── t_user_payment_method          用户收付款方式
│   └── t_crypto_deposit_address       加密充值地址池 (P2)
│
├── 【业务订单】
│   ├── t_deposit_order                入金订单
│   ├── t_withdrawal_order             出金订单
│   ├── t_internal_transfer            内部转账
│   ├── t_payout_instruction           统一出款指令
│   └── t_chargeback                   拒付/退单
│
├── 【原路退回】
│   ├── t_deposit_lot                  入金批次(权威)
│   └── t_deposit_lot_consumption      批次消耗流水
│
├── 【冻结与账务】
│   ├── t_account_fund_freeze          资金冻结明细
│   ├── t_account_ledger               客户资金流水(引擎写)
│   ├── t_journal_entry                公司分录头
│   └── t_journal_line                 公司分录行
│
├── 【风控与审批】
│   ├── t_fund_risk_rule               资金风控规则
│   ├── t_fund_risk_check_log          风控命中日志
│   └── t_fund_approval                人工审核任务
│
├── 【基础设施】
│   ├── t_fund_idempotency             幂等记录
│   ├── t_fund_outbox                  领域事件发件箱
│   ├── t_exchange_rate_snapshot       汇率使用快照
│   ├── t_fund_reconciliation          PSP对账汇总
│   └── t_fund_reconciliation_detail   PSP对账明细
```

> **废弃：** `t_deposit_source_tracker` 不再作为权威；如需运营看板可对 lot 做物化汇总，但出金分配必须以 lot 为准。

---

## 九、核心表 SQL

### 9.1 支付通道 `t_payment_channel`

```sql
CREATE TABLE `t_payment_channel` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '监管实体ID,通道按实体隔离配置与结算账户',
    `channel_code`          VARCHAR(32) NOT NULL COMMENT '通道代码: STRIPE_CARD, SKRILL, BANK_WIRE_INT, CHAIN_TRC20_USDT',
    `channel_name`          VARCHAR(128) NOT NULL COMMENT '面向用户名称',
    `description`           VARCHAR(512) DEFAULT NULL,
    `logo_url`              VARCHAR(256) DEFAULT NULL,

    `channel_type`          TINYINT UNSIGNED NOT NULL
        COMMENT '1-银行转账 2-卡 3-电子钱包 4-本地支付 5-加密链上',
    `direction`             TINYINT UNSIGNED NOT NULL DEFAULT 3
        COMMENT '1-仅入金 2-仅出金 3-双向',

    `provider_code`         VARCHAR(32) NOT NULL COMMENT 'STRIPE/SKRILL/BANK_MANUAL/CHAIN_MONITOR',
    `provider_config_ref`   VARCHAR(128) DEFAULT NULL
        COMMENT '密钥与商户配置引用(KMS/Vault路径),禁止明文落库',

    `blockchain_network`    VARCHAR(16) DEFAULT NULL COMMENT 'BTC/ETH/TRX/BSC/SOL',
    `token_contract`        VARCHAR(128) DEFAULT NULL,
    `min_confirmations`     INT UNSIGNED DEFAULT NULL,

    `supported_currencies`  VARCHAR(256) NOT NULL COMMENT 'USD,EUR 或 USDT',
    `min_amount`            DECIMAL(18,4) NOT NULL DEFAULT 0,
    `max_amount`            DECIMAL(18,4) DEFAULT NULL,
    `daily_limit`           DECIMAL(18,4) DEFAULT NULL,

    `fee_type`              TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-免费 1-固定 2-比例 3-固定+比例',
    `fee_fixed`             DECIMAL(18,4) DEFAULT NULL,
    `fee_rate`              DECIMAL(8,6) DEFAULT NULL,
    `fee_min`               DECIMAL(18,4) DEFAULT NULL,
    `fee_max`               DECIMAL(18,4) DEFAULT NULL,
    `fee_currency`          VARCHAR(8) DEFAULT NULL,
    `fee_bearer`            TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-用户 2-券商 3-各半',

    `estimated_arrival`     VARCHAR(64) DEFAULT NULL,
    `processing_hours`      VARCHAR(128) DEFAULT NULL,
    `allowed_countries`     TEXT DEFAULT NULL,
    `blocked_countries`     TEXT DEFAULT NULL,
    `allowed_kyc_levels`    VARCHAR(16) DEFAULT NULL COMMENT '如1,2,3',

    `cooldown_hours`        INT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '入金后出金冷静期(小时);卡类通常72',

    `client_money_bucket`   VARCHAR(32) NOT NULL DEFAULT 'SEGREGATED'
        COMMENT '客户资金归集桶: SEGREGATED/OMNIBUS/COMPANY(仅费用)',

    `sort_order`            INT NOT NULL DEFAULT 0,
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '0-停用 1-启用 2-维护中',
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_entity_channel` (`entity_id`, `channel_code`),
    KEY `idx_type_status` (`channel_type`, `status`),
    KEY `idx_provider` (`provider_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='支付通道定义';
```

### 9.2 用户收付款方式 `t_user_payment_method`

```sql
CREATE TABLE `t_user_payment_method` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `method_type`           TINYINT UNSIGNED NOT NULL
        COMMENT '1-银行 2-卡 3-电子钱包 4-加密地址',
    `label`                 VARCHAR(64) DEFAULT NULL,

    `bank_country`          VARCHAR(4) DEFAULT NULL,
    `bank_name`             VARCHAR(128) DEFAULT NULL,
    `bank_swift`            VARCHAR(16) DEFAULT NULL,
    `bank_branch_code`      VARCHAR(32) DEFAULT NULL,
    `bank_account_number`   VARCHAR(256) DEFAULT NULL COMMENT '加密存储',
    `bank_iban`             VARCHAR(64) DEFAULT NULL,
    `bank_account_name`     VARCHAR(128) DEFAULT NULL COMMENT '必须与KYC姓名一致',
    `bank_currency`         VARCHAR(8) DEFAULT NULL,

    `card_brand`            VARCHAR(16) DEFAULT NULL,
    `card_last_four`        VARCHAR(4) DEFAULT NULL,
    `card_token`            VARCHAR(256) DEFAULT NULL COMMENT 'PSP token,加密存储;禁止存全卡号',
    `card_expiry`           VARCHAR(8) DEFAULT NULL COMMENT '加密存储',
    `card_holder_name`      VARCHAR(128) DEFAULT NULL,

    `ewallet_provider`      VARCHAR(32) DEFAULT NULL,
    `ewallet_account`       VARCHAR(256) DEFAULT NULL COMMENT '加密存储',

    `crypto_network`        VARCHAR(16) DEFAULT NULL,
    `crypto_address`        VARCHAR(256) DEFAULT NULL,
    `crypto_address_tag`    VARCHAR(64) DEFAULT NULL,
    `crypto_currency`       VARCHAR(16) DEFAULT NULL,

    `name_match_status`     TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-未校验 1-同名通过 2-同名失败 3-不适用(加密)',
    `is_verified`           TINYINT UNSIGNED NOT NULL DEFAULT 0,
    `verified_at`           DATETIME(3) DEFAULT NULL,
    `is_default`            TINYINT UNSIGNED NOT NULL DEFAULT 0,
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '0-删除 1-活跃 2-过期',
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    KEY `idx_user_type` (`user_id`, `method_type`, `status`),
    KEY `idx_entity_user` (`entity_id`, `user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='用户收付款方式';
```

### 9.3 入金订单 `t_deposit_order`

```sql
CREATE TABLE `t_deposit_order` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `deposit_no`            VARCHAR(64) NOT NULL COMMENT '业务单号(雪花)',
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `account_no`            VARCHAR(32) NOT NULL,

    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `channel_code`          VARCHAR(32) NOT NULL,
    `payment_method_id`     BIGINT UNSIGNED DEFAULT NULL,

    `request_amount`        DECIMAL(18,4) NOT NULL,
    `request_currency`      VARCHAR(8) NOT NULL,
    `fee_amount`            DECIMAL(18,4) NOT NULL DEFAULT 0,
    `fee_currency`          VARCHAR(8) DEFAULT NULL,
    `actual_amount`         DECIMAL(18,4) DEFAULT NULL COMMENT 'PSP确认金额',
    `actual_currency`       VARCHAR(8) DEFAULT NULL,
    `exchange_rate`         DECIMAL(18,8) DEFAULT NULL,
    `credit_amount`         DECIMAL(18,4) DEFAULT NULL COMMENT '入账账户币种金额',
    `credit_currency`       VARCHAR(8) DEFAULT NULL,

    `crypto_address`        VARCHAR(256) DEFAULT NULL,
    `crypto_address_tag`    VARCHAR(64) DEFAULT NULL,
    `crypto_tx_hash`        VARCHAR(128) DEFAULT NULL,
    `crypto_confirmations`  INT UNSIGNED DEFAULT NULL,
    `crypto_network`        VARCHAR(16) DEFAULT NULL,

    `psp_order_id`          VARCHAR(128) DEFAULT NULL,
    `psp_redirect_url`      VARCHAR(1024) DEFAULT NULL,
    `psp_callback_raw`      MEDIUMTEXT DEFAULT NULL,

    `risk_check_result`     TINYINT UNSIGNED DEFAULT NULL
        COMMENT '1-自动通过 2-人工中 3-人工通过 4-拒绝',
    `risk_check_remark`     VARCHAR(512) DEFAULT NULL,
    `approval_id`           BIGINT UNSIGNED DEFAULT NULL COMMENT '关联t_fund_approval',

    `ledger_id`             BIGINT UNSIGNED DEFAULT NULL,
    `journal_entry_id`      BIGINT UNSIGNED DEFAULT NULL,
    `idempotency_key`       VARCHAR(128) NOT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-CREATED 1-PENDING_PAY 2-PAID 3-CONFIRMING 4-CREDITING 5-CREDITED 6-COMPLETED 7-FAILED 8-EXPIRED 9-CANCELLED 10-REJECTED',
    `fail_reason`           VARCHAR(256) DEFAULT NULL,
    `expire_at`             DATETIME(3) DEFAULT NULL,

    `source`                VARCHAR(16) NOT NULL DEFAULT 'WEB',
    `client_ip`             VARCHAR(45) DEFAULT NULL,
    `remark`                VARCHAR(256) DEFAULT NULL,

    `paid_at`               DATETIME(3) DEFAULT NULL,
    `credited_at`           DATETIME(3) DEFAULT NULL,
    `completed_at`          DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_deposit_no` (`deposit_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    UNIQUE KEY `uk_psp_order` (`channel_id`, `psp_order_id`),
    KEY `idx_user` (`user_id`, `created_at`),
    KEY `idx_account` (`account_id`, `created_at`),
    KEY `idx_status` (`status`, `created_at`),
    KEY `idx_entity_status` (`entity_id`, `status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='入金订单';
```

### 9.4 出金订单 `t_withdrawal_order`

```sql
CREATE TABLE `t_withdrawal_order` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `withdrawal_no`         VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `account_no`            VARCHAR(32) NOT NULL,

    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `channel_code`          VARCHAR(32) NOT NULL,
    `payment_method_id`     BIGINT UNSIGNED NOT NULL,

    `request_amount`        DECIMAL(18,4) NOT NULL COMMENT '申请金额(账户币种)',
    `request_currency`      VARCHAR(8) NOT NULL,
    `fee_amount`            DECIMAL(18,4) NOT NULL DEFAULT 0,
    `fee_currency`          VARCHAR(8) DEFAULT NULL,
    `actual_amount`         DECIMAL(18,4) DEFAULT NULL COMMENT '扣费后出款金额',
    `actual_currency`       VARCHAR(8) DEFAULT NULL,
    `exchange_rate`         DECIMAL(18,8) DEFAULT NULL,
    `payout_amount`         DECIMAL(18,4) DEFAULT NULL COMMENT '用户实际收到',
    `payout_currency`       VARCHAR(8) DEFAULT NULL,

    `principal_amount`      DECIMAL(18,4) NOT NULL DEFAULT 0 COMMENT '原路本金部分',
    `profit_amount`         DECIMAL(18,4) NOT NULL DEFAULT 0 COMMENT '盈利自选部分',
    `target_info_snapshot`  TEXT DEFAULT NULL COMMENT '出金目标快照JSON',

    `crypto_tx_hash`        VARCHAR(128) DEFAULT NULL,
    `crypto_network`        VARCHAR(16) DEFAULT NULL,
    `crypto_address`        VARCHAR(256) DEFAULT NULL,
    `crypto_confirmations`  INT UNSIGNED DEFAULT NULL,

    `payout_instruction_id` BIGINT UNSIGNED DEFAULT NULL,
    `psp_order_id`          VARCHAR(128) DEFAULT NULL,
    `psp_callback_raw`      MEDIUMTEXT DEFAULT NULL,

    `risk_check_result`     TINYINT UNSIGNED DEFAULT NULL,
    `risk_check_remark`     VARCHAR(512) DEFAULT NULL,
    `approval_id`           BIGINT UNSIGNED DEFAULT NULL,

    `balance_before`        DECIMAL(18,4) NOT NULL,
    `equity_before`         DECIMAL(18,4) NOT NULL,
    `margin_used_before`    DECIMAL(18,4) NOT NULL DEFAULT 0,
    `margin_level_before`   DECIMAL(12,2) DEFAULT NULL,
    `withdrawable_before`   DECIMAL(18,4) NOT NULL,

    `freeze_id`             BIGINT UNSIGNED DEFAULT NULL,
    `ledger_id`             BIGINT UNSIGNED DEFAULT NULL,
    `journal_entry_id`      BIGINT UNSIGNED DEFAULT NULL,
    `idempotency_key`       VARCHAR(128) NOT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-CREATED 1-FROZEN 2-PENDING_REVIEW 3-APPROVED 4-PAYOUT_SUBMITTED 5-COMPLETED 6-REJECTED 7-FAILED 8-CANCELLED',
    `fail_reason`           VARCHAR(256) DEFAULT NULL,
    `retry_count`           INT UNSIGNED NOT NULL DEFAULT 0,

    `source`                VARCHAR(16) NOT NULL DEFAULT 'WEB',
    `client_ip`             VARCHAR(45) DEFAULT NULL,
    `remark`                VARCHAR(256) DEFAULT NULL,
    `admin_remark`          VARCHAR(256) DEFAULT NULL,

    `approved_at`           DATETIME(3) DEFAULT NULL,
    `processing_at`         DATETIME(3) DEFAULT NULL,
    `completed_at`          DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_withdrawal_no` (`withdrawal_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    KEY `idx_user` (`user_id`, `created_at`),
    KEY `idx_account` (`account_id`, `created_at`),
    KEY `idx_status` (`status`, `created_at`),
    KEY `idx_entity_status` (`entity_id`, `status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='出金订单';
```

### 9.5 内部转账 `t_internal_transfer`

```sql
CREATE TABLE `t_internal_transfer` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `transfer_no`           VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,

    `from_account_id`       BIGINT UNSIGNED NOT NULL,
    `from_account_no`       VARCHAR(32) NOT NULL,
    `from_currency`         VARCHAR(8) NOT NULL,
    `from_amount`           DECIMAL(18,4) NOT NULL,

    `to_account_id`         BIGINT UNSIGNED NOT NULL,
    `to_account_no`         VARCHAR(32) NOT NULL,
    `to_currency`           VARCHAR(8) NOT NULL,
    `to_amount`             DECIMAL(18,4) NOT NULL,

    `exchange_rate`         DECIMAL(18,8) DEFAULT NULL,
    `rate_snapshot_id`      BIGINT UNSIGNED DEFAULT NULL,

    `from_ledger_id`        BIGINT UNSIGNED DEFAULT NULL,
    `to_ledger_id`          BIGINT UNSIGNED DEFAULT NULL,
    `journal_entry_id`      BIGINT UNSIGNED DEFAULT NULL,
    `idempotency_key`       VARCHAR(128) NOT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-CREATED 1-PROCESSING 2-COMPLETED 3-FAILED',
    `fail_reason`           VARCHAR(256) DEFAULT NULL,
    `source`                VARCHAR(16) NOT NULL DEFAULT 'WEB',
    `remark`                VARCHAR(256) DEFAULT NULL,
    `client_ip`             VARCHAR(45) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_transfer_no` (`transfer_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    KEY `idx_user` (`user_id`, `created_at`),
    KEY `idx_from` (`from_account_id`, `created_at`),
    KEY `idx_to` (`to_account_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='内部转账';
```

### 9.6 统一出款指令 `t_payout_instruction`

```sql
CREATE TABLE `t_payout_instruction` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `instruction_no`        VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `source_type`           VARCHAR(32) NOT NULL
        COMMENT 'CUSTOMER_WITHDRAWAL / PARTNER_WALLET / CHARGEBACK_REFUND',
    `source_id`             BIGINT UNSIGNED NOT NULL COMMENT '来源业务ID',
    `user_id`               BIGINT UNSIGNED DEFAULT NULL COMMENT '客户出金时',
    `partner_id`            BIGINT UNSIGNED DEFAULT NULL COMMENT '代理出款时',

    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `payment_method_id`     BIGINT UNSIGNED DEFAULT NULL,
    `target_snapshot`       TEXT NOT NULL COMMENT '出款目标JSON快照',

    `amount`                DECIMAL(18,4) NOT NULL,
    `currency`              VARCHAR(8) NOT NULL,
    `fee_amount`            DECIMAL(18,4) NOT NULL DEFAULT 0,

    `psp_order_id`          VARCHAR(128) DEFAULT NULL,
    `psp_batch_id`          VARCHAR(64) DEFAULT NULL,
    `psp_callback_raw`      MEDIUMTEXT DEFAULT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-CREATED 1-SUBMITTED 2-SUCCEEDED 3-FAILED 4-CANCELLED',
    `fail_reason`           VARCHAR(256) DEFAULT NULL,
    `retry_count`           INT UNSIGNED NOT NULL DEFAULT 0,
    `idempotency_key`       VARCHAR(128) NOT NULL,

    `submitted_at`          DATETIME(3) DEFAULT NULL,
    `succeeded_at`          DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_instruction_no` (`instruction_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    KEY `idx_source` (`source_type`, `source_id`),
    KEY `idx_status` (`status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='统一出款指令(客户出金/Partner出款共用适配器)';
```

### 9.7 入金批次 `t_deposit_lot`（原路退回权威）

```sql
CREATE TABLE `t_deposit_lot` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `lot_no`                VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `deposit_id`            BIGINT UNSIGNED NOT NULL,
    `deposit_no`            VARCHAR(64) NOT NULL,

    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `payment_method_id`     BIGINT UNSIGNED DEFAULT NULL,
    `currency`              VARCHAR(8) NOT NULL COMMENT '账户结算币种',

    `original_amount`       DECIMAL(18,4) NOT NULL COMMENT '入账金额',
    `remaining_amount`      DECIMAL(18,4) NOT NULL COMMENT '剩余可退本金',
    `reserved_amount`       DECIMAL(18,4) NOT NULL DEFAULT 0 COMMENT '出金预占中',
    `consumed_amount`       DECIMAL(18,4) NOT NULL DEFAULT 0 COMMENT '已消耗(出金+拒付)',

    `cooldown_until`        DATETIME(3) DEFAULT NULL COMMENT '该批次冷静期截止',
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-OPEN 2-FULLY_CONSUMED 3-CHARGEBACK 4-CLOSED',

    `credited_at`           DATETIME(3) NOT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_lot_no` (`lot_no`),
    UNIQUE KEY `uk_deposit` (`deposit_id`),
    KEY `idx_fifo` (`user_id`, `currency`, `status`, `credited_at`),
    KEY `idx_channel_method` (`user_id`, `channel_id`, `payment_method_id`, `status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='入金批次(原路退回FIFO权威)';
```

### 9.8 批次消耗 `t_deposit_lot_consumption`

```sql
CREATE TABLE `t_deposit_lot_consumption` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `lot_id`                BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,

    `consume_type`          VARCHAR(32) NOT NULL
        COMMENT 'WITHDRAWAL / CHARGEBACK / ADJUSTMENT',
    `reference_type`        VARCHAR(32) NOT NULL COMMENT 'WITHDRAWAL/CHARGEBACK/...',
    `reference_id`          BIGINT UNSIGNED NOT NULL,
    `reference_no`          VARCHAR(64) NOT NULL,

    `amount`                DECIMAL(18,4) NOT NULL,
    `currency`              VARCHAR(8) NOT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-RESERVED 2-CONSUMED 3-RELEASED',
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    KEY `idx_lot` (`lot_id`, `status`),
    KEY `idx_ref` (`reference_type`, `reference_id`),
    KEY `idx_user` (`user_id`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='入金批次消耗流水';
```

### 9.9 拒付 `t_chargeback`

```sql
CREATE TABLE `t_chargeback` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `chargeback_no`         VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `deposit_id`            BIGINT UNSIGNED NOT NULL,
    `deposit_no`            VARCHAR(64) NOT NULL,
    `lot_id`                BIGINT UNSIGNED DEFAULT NULL,

    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `psp_chargeback_id`     VARCHAR(128) DEFAULT NULL,
    `amount`                DECIMAL(18,4) NOT NULL,
    `currency`              VARCHAR(8) NOT NULL,
    `reason_code`           VARCHAR(64) DEFAULT NULL,
    `reason_message`        VARCHAR(512) DEFAULT NULL,

    `recovered_amount`      DECIMAL(18,4) NOT NULL DEFAULT 0,
    `deficit_amount`        DECIMAL(18,4) NOT NULL DEFAULT 0,
    `ledger_id`             BIGINT UNSIGNED DEFAULT NULL,
    `journal_entry_id`      BIGINT UNSIGNED DEFAULT NULL,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-OPEN 1-RECOVERING 2-FUNDS_RECOVERED 3-DEFICIT_CREATED 4-CLOSED',
    `idempotency_key`       VARCHAR(128) NOT NULL,
    `opened_at`             DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `closed_at`             DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_chargeback_no` (`chargeback_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    KEY `idx_deposit` (`deposit_id`),
    KEY `idx_user` (`user_id`, `created_at`),
    KEY `idx_status` (`status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='拒付/退单';
```

### 9.10 资金冻结 `t_account_fund_freeze`

```sql
CREATE TABLE `t_account_fund_freeze` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `freeze_type`           TINYINT UNSIGNED NOT NULL
        COMMENT '1-出金 2-调账 3-风控 4-争议/拒付',
    `freeze_amount`         DECIMAL(18,4) NOT NULL,
    `currency`              VARCHAR(8) NOT NULL,
    `reference_type`        VARCHAR(32) NOT NULL COMMENT 'WITHDRAWAL/ADJUSTMENT/RISK/CHARGEBACK',
    `reference_id`          BIGINT UNSIGNED DEFAULT NULL,
    `reference_no`          VARCHAR(64) DEFAULT NULL,
    `engine_request_id`     VARCHAR(128) NOT NULL COMMENT 'FreezeFunds的request_id',

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-冻结中 2-已释放 3-已扣减(随Debit完成)',
    `released_at`           DATETIME(3) DEFAULT NULL,
    `release_reason`        VARCHAR(128) DEFAULT NULL
        COMMENT 'COMPLETED/REJECTED/CANCELLED/FAILED/ADMIN',
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_engine_request` (`engine_request_id`),
    KEY `idx_account_status` (`account_id`, `status`),
    KEY `idx_reference` (`reference_type`, `reference_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='账户资金冻结明细';
```

### 9.11 客户资金流水 `t_account_ledger`（引擎写，资金域定义结构）

```sql
CREATE TABLE `t_account_ledger` (
    `id`                BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `ledger_no`         VARCHAR(64) NOT NULL,
    `entity_id`         BIGINT UNSIGNED NOT NULL,
    `account_id`        BIGINT UNSIGNED NOT NULL,

    `ledger_type`       TINYINT UNSIGNED NOT NULL COMMENT '
        1-DEPOSIT
        2-WITHDRAWAL
        3-TRADE_PROFIT
        4-COMMISSION
        5-SWAP
        6-CREDIT_IN
        7-CREDIT_OUT
        8-ADJUSTMENT
        9-DIVIDEND
        10-FEE
        11-TRANSFER_OUT
        12-TRANSFER_IN
        13-CHARGEBACK
        14-NEGATIVE_BALANCE_CORRECTION
        15-PARTNER_TRANSFER',

    `direction`         TINYINT NOT NULL COMMENT '1-入账 -1-出账',
    `amount`            DECIMAL(18,4) NOT NULL COMMENT '绝对值,恒正',
    `balance_before`    DECIMAL(18,4) NOT NULL,
    `balance_after`     DECIMAL(18,4) NOT NULL,
    `currency`          VARCHAR(8) NOT NULL,

    `deal_id`           BIGINT UNSIGNED DEFAULT NULL,
    `deal_ticket`       BIGINT UNSIGNED DEFAULT NULL,
    `position_ticket`   BIGINT UNSIGNED DEFAULT NULL,
    `order_ticket`      BIGINT UNSIGNED DEFAULT NULL,

    `deposit_id`        BIGINT UNSIGNED DEFAULT NULL,
    `withdrawal_id`     BIGINT UNSIGNED DEFAULT NULL,
    `transfer_id`       BIGINT UNSIGNED DEFAULT NULL,
    `chargeback_id`     BIGINT UNSIGNED DEFAULT NULL,
    `journal_entry_id`  BIGINT UNSIGNED DEFAULT NULL,

    `reference_no`      VARCHAR(64) DEFAULT NULL,
    `idempotency_key`   VARCHAR(128) NOT NULL COMMENT '与引擎request_id一致',
    `remark`            VARCHAR(256) DEFAULT NULL,
    `operator_id`       BIGINT UNSIGNED DEFAULT NULL,

    `created_at`        DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_ledger_no` (`ledger_no`),
    UNIQUE KEY `uk_idempotency` (`account_id`, `idempotency_key`),
    KEY `idx_account_time` (`account_id`, `created_at`),
    KEY `idx_entity_time` (`entity_id`, `created_at`),
    KEY `idx_type` (`ledger_type`, `created_at`),
    KEY `idx_deposit` (`deposit_id`),
    KEY `idx_withdrawal` (`withdrawal_id`),
    KEY `idx_transfer` (`transfer_id`),
    KEY `idx_chargeback` (`chargeback_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='客户资金流水(余额变动唯一入口,引擎写入)';
```

### 9.12 公司账簿 `t_journal_entry` / `t_journal_line`

```sql
CREATE TABLE `t_journal_entry` (
    `id`                BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entry_no`          VARCHAR(64) NOT NULL,
    `entity_id`         BIGINT UNSIGNED NOT NULL,
    `book_date`         DATE NOT NULL COMMENT '账务日',
    `value_date`        DATE NOT NULL COMMENT '起息日',
    `source_type`       VARCHAR(32) NOT NULL
        COMMENT 'DEPOSIT/WITHDRAWAL/TRANSFER/FEE/FX/CHARGEBACK/PAYOUT/MANUAL',
    `source_id`         BIGINT UNSIGNED DEFAULT NULL,
    `source_no`         VARCHAR(64) DEFAULT NULL,
    `description`       VARCHAR(256) DEFAULT NULL,
    `status`            TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-POSTED 2-REVERSED',
    `reversed_by`       BIGINT UNSIGNED DEFAULT NULL,
    `idempotency_key`   VARCHAR(128) NOT NULL,
    `created_by`        BIGINT UNSIGNED DEFAULT NULL,
    `created_at`        DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_entry_no` (`entry_no`),
    UNIQUE KEY `uk_idempotency` (`entity_id`, `idempotency_key`),
    KEY `idx_book` (`entity_id`, `book_date`),
    KEY `idx_source` (`source_type`, `source_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='公司账簿分录头';

CREATE TABLE `t_journal_line` (
    `id`                BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entry_id`          BIGINT UNSIGNED NOT NULL,
    `entity_id`         BIGINT UNSIGNED NOT NULL,
    `line_no`           INT UNSIGNED NOT NULL,
    `account_code`      VARCHAR(32) NOT NULL COMMENT '
        CLIENT_MONEY_LIABILITY  客户资金负债
        PSP_RECEIVABLE          PSP应收
        PSP_PAYABLE             PSP应付
        BANK_CLEARING           银行清算
        FEE_INCOME              手续费收入
        FX_GAIN_LOSS            汇兑损益
        COMPANY_CASH            公司自有资金
        CHARGEBACK_LOSS         拒付损失',
    `dc_flag`           CHAR(1) NOT NULL COMMENT 'D-借 C-贷',
    `amount`            DECIMAL(18,4) NOT NULL,
    `currency`          VARCHAR(8) NOT NULL,
    `memo`              VARCHAR(256) DEFAULT NULL,
    `created_at`        DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_entry_line` (`entry_id`, `line_no`),
    KEY `idx_account_code` (`entity_id`, `account_code`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='公司账簿分录行(复式,同entry借贷平衡)';
```

**分录示例（入金完成，用户承担手续费）：**

```
借: PSP_RECEIVABLE     actual_amount
贷: CLIENT_MONEY_LIABILITY  credit_amount
贷: FEE_INCOME              fee_amount
```

**分录示例（出金完成）：**

```
借: CLIENT_MONEY_LIABILITY  request_amount
贷: PSP_PAYABLE             payout_amount
贷: FEE_INCOME              fee_amount   (若收取)
```

### 9.13 资金风控规则 `t_fund_risk_rule`

```sql
CREATE TABLE `t_fund_risk_rule` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL DEFAULT 1,
    `rule_code`             VARCHAR(64) NOT NULL,
    `rule_name`             VARCHAR(128) NOT NULL,
    `description`           VARCHAR(512) DEFAULT NULL,
    `direction`             TINYINT UNSIGNED NOT NULL COMMENT '1-入金 2-出金 3-两者',

    `scope_type`            VARCHAR(16) NOT NULL DEFAULT 'GLOBAL'
        COMMENT 'GLOBAL/KYC_LEVEL/CHANNEL/USER/ACCOUNT',
    `scope_ref_id`          BIGINT UNSIGNED DEFAULT NULL,
    `scope_kyc_level`       TINYINT UNSIGNED DEFAULT NULL,

    `rule_type`             VARCHAR(32) NOT NULL COMMENT '
        AMOUNT_LIMIT_SINGLE
        AMOUNT_LIMIT_DAILY
        AMOUNT_LIMIT_MONTHLY
        AMOUNT_LIMIT_TOTAL
        FREQUENCY_LIMIT_DAILY
        COOLDOWN_HOURS
        MANUAL_REVIEW_ABOVE
        FIRST_OPERATION_REVIEW
        PASSWORD_CHANGE_LOCK
        RETURN_PATH_REQUIRED',
    `threshold_amount`      DECIMAL(18,4) DEFAULT NULL,
    `threshold_count`       INT UNSIGNED DEFAULT NULL,
    `threshold_hours`       INT UNSIGNED DEFAULT NULL,
    `threshold_currency`    VARCHAR(8) DEFAULT NULL COMMENT '限额折算币种,如USD',

    `action_type`           TINYINT UNSIGNED NOT NULL DEFAULT 1
        COMMENT '1-拒绝 2-人工审核 3-标记可疑 4-通知管理员',
    `priority`              INT NOT NULL DEFAULT 0 COMMENT '越大越先匹配',
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 1,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_entity_rule` (`entity_id`, `rule_code`),
    KEY `idx_match` (`entity_id`, `direction`, `status`, `priority` DESC)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='资金风控规则';
```

匹配优先级：`ACCOUNT > USER > KYC_LEVEL > CHANNEL > GLOBAL`，同 scope 按 `priority` 降序。

### 9.14 风控命中日志 `t_fund_risk_check_log`

```sql
CREATE TABLE `t_fund_risk_check_log` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `user_id`               BIGINT UNSIGNED NOT NULL,
    `account_id`            BIGINT UNSIGNED NOT NULL,
    `order_type`            VARCHAR(16) NOT NULL COMMENT 'DEPOSIT/WITHDRAWAL/TRANSFER',
    `order_id`              BIGINT UNSIGNED NOT NULL,
    `order_no`              VARCHAR(64) NOT NULL,
    `request_amount`        DECIMAL(18,4) NOT NULL,
    `request_currency`      VARCHAR(8) NOT NULL,
    `rule_id`               BIGINT UNSIGNED NOT NULL,
    `rule_code`             VARCHAR(64) NOT NULL,
    `rule_type`             VARCHAR(32) NOT NULL,
    `current_value`         VARCHAR(128) DEFAULT NULL,
    `threshold_value`       VARCHAR(128) DEFAULT NULL,
    `check_result`          TINYINT UNSIGNED NOT NULL
        COMMENT '1-通过 2-拒绝 3-需人工审核 4-标记可疑',
    `auto_resolved`         TINYINT UNSIGNED NOT NULL DEFAULT 0,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    KEY `idx_order` (`order_type`, `order_id`),
    KEY `idx_user` (`user_id`, `created_at`),
    KEY `idx_result` (`check_result`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='资金风控检查日志';
```

### 9.15 人工审核 `t_fund_approval`

```sql
CREATE TABLE `t_fund_approval` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `approval_no`           VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `biz_type`              VARCHAR(32) NOT NULL
        COMMENT 'DEPOSIT/WITHDRAWAL/ADJUSTMENT/PAYOUT',
    `biz_id`                BIGINT UNSIGNED NOT NULL,
    `biz_no`                VARCHAR(64) NOT NULL,
    `amount`                DECIMAL(18,4) NOT NULL,
    `currency`              VARCHAR(8) NOT NULL,

    `requester_id`          BIGINT UNSIGNED NOT NULL COMMENT '提交人/系统',
    `approver_id`           BIGINT UNSIGNED DEFAULT NULL COMMENT '审批人,必须≠requester',
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-PENDING 1-APPROVED 2-REJECTED 3-CANCELLED',
    `decision_remark`       VARCHAR(512) DEFAULT NULL,
    `decided_at`            DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_approval_no` (`approval_no`),
    KEY `idx_biz` (`biz_type`, `biz_id`),
    KEY `idx_pending` (`entity_id`, `status`, `created_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='资金人工审核(四眼)';
```

### 9.16 幂等 `t_fund_idempotency`

```sql
CREATE TABLE `t_fund_idempotency` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `scope`                 VARCHAR(64) NOT NULL
        COMMENT 'DEPOSIT_CREATE/PSP_WEBHOOK/WITHDRAWAL_CREATE/ENGINE_CREDIT/...',
    `idempotency_key`       VARCHAR(128) NOT NULL,
    `request_hash`          CHAR(64) NOT NULL COMMENT '请求体SHA256',
    `response_body`         MEDIUMTEXT DEFAULT NULL,
    `http_status`           INT UNSIGNED DEFAULT NULL,
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-PROCESSING 1-DONE 2-FAILED',
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_scope_key` (`entity_id`, `scope`, `idempotency_key`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='资金幂等记录';
```

### 9.17 事件发件箱 `t_fund_outbox`

```sql
CREATE TABLE `t_fund_outbox` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `event_id`              VARCHAR(64) NOT NULL,
    `event_type`            VARCHAR(64) NOT NULL,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `aggregate_type`        VARCHAR(32) NOT NULL,
    `aggregate_id`          BIGINT UNSIGNED NOT NULL,
    `aggregate_version`     INT UNSIGNED NOT NULL DEFAULT 1,
    `correlation_id`        VARCHAR(64) DEFAULT NULL,
    `payload`               JSON NOT NULL,
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-PENDING 1-SENT 2-FAILED',
    `retry_count`           INT UNSIGNED NOT NULL DEFAULT 0,
    `next_retry_at`         DATETIME(3) DEFAULT NULL,
    `sent_at`               DATETIME(3) DEFAULT NULL,
    `occurred_at`           DATETIME(3) NOT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_event_id` (`event_id`),
    KEY `idx_poll` (`status`, `next_retry_at`, `id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='资金领域事件发件箱';
```

### 9.18 汇率快照 `t_exchange_rate_snapshot`

```sql
CREATE TABLE `t_exchange_rate_snapshot` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `from_currency`         VARCHAR(8) NOT NULL,
    `to_currency`           VARCHAR(8) NOT NULL,
    `rate`                  DECIMAL(18,8) NOT NULL COMMENT '含加点后成交汇率',
    `rate_source`           VARCHAR(32) NOT NULL
        COMMENT 'MARKET_BID/MARKET_ASK/MARKET_MID/ECB/MANUAL',
    `markup_pct`            DECIMAL(8,4) NOT NULL DEFAULT 0,
    `rate_before_markup`    DECIMAL(18,8) DEFAULT NULL,
    `reference_type`        VARCHAR(16) NOT NULL
        COMMENT 'DEPOSIT/WITHDRAWAL/TRANSFER/SWAP/COMMISSION',
    `reference_id`          BIGINT UNSIGNED NOT NULL,
    `snapshot_at`           DATETIME(3) NOT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    KEY `idx_reference` (`reference_type`, `reference_id`),
    KEY `idx_pair` (`from_currency`, `to_currency`, `snapshot_at`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='汇率使用快照(审计)';
```

### 9.19 PSP 对账

```sql
CREATE TABLE `t_fund_reconciliation` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `recon_date`            DATE NOT NULL,
    `channel_id`            BIGINT UNSIGNED NOT NULL,
    `channel_code`          VARCHAR(32) NOT NULL,
    `direction`             TINYINT UNSIGNED NOT NULL COMMENT '1-入金 2-出金',

    `sys_count`             INT UNSIGNED NOT NULL DEFAULT 0,
    `sys_total_amount`      DECIMAL(18,4) NOT NULL DEFAULT 0,
    `sys_currency`          VARCHAR(8) NOT NULL,
    `psp_count`             INT UNSIGNED NOT NULL DEFAULT 0,
    `psp_total_amount`      DECIMAL(18,4) NOT NULL DEFAULT 0,
    `psp_currency`          VARCHAR(8) NOT NULL,
    `diff_count`            INT NOT NULL DEFAULT 0,
    `diff_amount`           DECIMAL(18,4) NOT NULL DEFAULT 0,
    `exception_count`       INT UNSIGNED NOT NULL DEFAULT 0,

    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-待处理 1-一致 2-有差异 3-已处理',
    `processed_by`          BIGINT UNSIGNED DEFAULT NULL,
    `processed_at`          DATETIME(3) DEFAULT NULL,
    `process_remark`        VARCHAR(512) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_date_channel_dir` (`entity_id`, `recon_date`, `channel_id`, `direction`),
    KEY `idx_status` (`status`, `recon_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='PSP对账汇总';

CREATE TABLE `t_fund_reconciliation_detail` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `recon_id`              BIGINT UNSIGNED NOT NULL,
    `sys_order_no`          VARCHAR(64) DEFAULT NULL,
    `sys_amount`            DECIMAL(18,4) DEFAULT NULL,
    `sys_status`            VARCHAR(16) DEFAULT NULL,
    `psp_order_id`          VARCHAR(128) DEFAULT NULL,
    `psp_amount`            DECIMAL(18,4) DEFAULT NULL,
    `psp_status`            VARCHAR(16) DEFAULT NULL,
    `diff_type`             VARCHAR(16) NOT NULL
        COMMENT 'MATCH/AMOUNT_DIFF/SYS_ONLY/PSP_ONLY/STATUS_DIFF',
    `diff_amount`           DECIMAL(18,4) DEFAULT NULL,
    `handle_status`         TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-待处理 1-已处理 2-忽略',
    `handle_remark`         VARCHAR(256) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    KEY `idx_recon` (`recon_id`),
    KEY `idx_diff` (`diff_type`, `handle_status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='PSP对账明细';
```

### 9.20 加密充值地址池 `t_crypto_deposit_address`（P2）

```sql
CREATE TABLE `t_crypto_deposit_address` (
    `id`                    BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
    `entity_id`             BIGINT UNSIGNED NOT NULL,
    `network`               VARCHAR(16) NOT NULL,
    `address`               VARCHAR(256) NOT NULL,
    `address_tag`           VARCHAR(64) DEFAULT NULL,
    `derivation_path`       VARCHAR(64) DEFAULT NULL,
    `key_index`             INT UNSIGNED DEFAULT NULL,
    `user_id`               BIGINT UNSIGNED DEFAULT NULL,
    `account_id`            BIGINT UNSIGNED DEFAULT NULL,
    `assigned_at`           DATETIME(3) DEFAULT NULL,
    `status`                TINYINT UNSIGNED NOT NULL DEFAULT 0
        COMMENT '0-空闲 1-已分配 2-停用',
    `last_scan_at`          DATETIME(3) DEFAULT NULL,
    `created_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3),
    `updated_at`            DATETIME(3) NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3),
    PRIMARY KEY (`id`),
    UNIQUE KEY `uk_network_address` (`network`, `address`),
    KEY `idx_user` (`user_id`, `network`),
    KEY `idx_status` (`status`, `network`, `entity_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci
COMMENT='加密充值地址池(P2)';
```

---

## 十、Ledger 全链路联动

### 10.1 写入方

| ledger_type | 写入方 | 触发时机 |
|-------------|--------|----------|
| DEPOSIT / WITHDRAWAL / TRANSFER_* / CHARGEBACK / ADJUSTMENT / CREDIT_* / FEE(资金侧) | 引擎（由 Fund 发起 RPC） | 出入金/转账/拒付/调账 |
| TRADE_PROFIT / COMMISSION / SWAP / DIVIDEND / FEE(交易侧) | 引擎（交易路径） | 成交/日切/公司行动 |

**任何路径最终都是引擎写 ledger**；Fund 与交易子系统都不直连改 balance。

### 10.2 入金入账链路

```
t_deposit_order: PAID → 风控通过 → CREDITING
  → Engine.CreditAccount(ledger_type=DEPOSIT, request_id=deposit_no)
  → t_account_ledger 新增一行, balance_after 更新
  → 引擎内存 balance 更新
  → Fund: 写 t_deposit_lot, journal, outbox, status=CREDITED/COMPLETED
```

### 10.3 出金完成链路

```
t_withdrawal_order: PAYOUT_SUBMITTED + 外部成功
  → Engine.DebitAccount(ledger_type=WITHDRAWAL, request_id=withdrawal_no:debit)
  → 同时将对应 freeze 标记为已扣减(引擎侧 frozen 减少)
  → Fund: lot CONSUMED, journal, outbox, COMPLETED
```

### 10.4 出金拒绝（无 ledger）

```
REJECTED → Engine.UnfreezeFunds → freeze RELEASED → lot RESERVED 释放
balance 不变
```

### 10.5 与交易 ledger 的一致性约束

任意时刻：

```
最后一条 ledger.balance_after == 引擎内存 balance == t_trading_account.balance（最终一致快照）
```

日切任务校验链式连续性与账户余额（见第十二章）。

---

## 十一、Redis 缓存（只读投影）

```
fund:available:{account_id}          Hash   展示用: balance/credit/frozen/available/withdrawable
                                       权威决策必须 GetAccountState / PreviewWithdraw

fund:rate:{user_id}:{dir}:{yyyyMMdd} Hash   当日次数与累计金额(USD折算), TTL 48h

fund:review:withdrawal:pending       ZSET   待审出金队列, score=提交时间

fund:crypto:addr:{network}:{address} Hash   地址→user/account 映射 (P2)

fund:channel:status:{channel_code}   Hash   熔断: ACTIVE/DEGRADED/DOWN, success_rate_1h...
```

---

## 十二、对账与审计校验

### 12.1 日切对账作业

1. 拉取 PSP Settlement / 银行对账文件  
2. 与当日 `COMPLETED` 入金/出金逐笔匹配 → `t_fund_reconciliation(_detail)`  
3. 校验 journal 借贷平衡  
4. 校验 `Σ CLIENT_MONEY_LIABILITY` 与客户 ledger 净入金口径（按实体）  
5. 差异工单人工处理，禁止直接改历史流水

### 12.2 审计 SQL

```sql
-- 校验1: 账户余额 vs 最后一条 ledger
SELECT a.id, a.account_no, a.balance AS account_balance, l.balance_after AS ledger_balance,
       CASE WHEN a.balance = l.balance_after THEN 'OK' ELSE 'MISMATCH' END AS check_result
FROM t_trading_account a
LEFT JOIN (
    SELECT account_id, balance_after,
           ROW_NUMBER() OVER (PARTITION BY account_id ORDER BY id DESC) AS rn
    FROM t_account_ledger
) l ON l.account_id = a.id AND l.rn = 1
WHERE a.status IN (0, 1, 2);

-- 校验2: ledger 链式连续
SELECT curr.id, curr.account_id, curr.balance_before, prev.balance_after,
       CASE WHEN curr.balance_before = prev.balance_after THEN 'OK' ELSE 'CHAIN_BROKEN' END AS check_result
FROM t_account_ledger curr
JOIN (
    SELECT id, account_id, balance_after,
           LEAD(id) OVER (PARTITION BY account_id ORDER BY id) AS next_id
    FROM t_account_ledger
) prev ON prev.next_id = curr.id AND prev.account_id = curr.account_id
HAVING check_result = 'CHAIN_BROKEN';

-- 校验3: 分录借贷平衡
SELECT e.id, e.entry_no,
       SUM(CASE WHEN l.dc_flag='D' THEN l.amount ELSE 0 END) AS debit_sum,
       SUM(CASE WHEN l.dc_flag='C' THEN l.amount ELSE 0 END) AS credit_sum
FROM t_journal_entry e
JOIN t_journal_line l ON l.entry_id = e.id
WHERE e.status = 1
GROUP BY e.id, e.entry_no
HAVING debit_sum <> credit_sum;

-- 校验4: lot 守恒
SELECT id, lot_no, original_amount, remaining_amount, reserved_amount, consumed_amount,
       CASE WHEN original_amount = remaining_amount + reserved_amount + consumed_amount
            THEN 'OK' ELSE 'LOT_BROKEN' END AS check_result
FROM t_deposit_lot;
```

---

## 十三、与周边模块关系

```
┌──────────────────────┐
│  Fund Service(本文)   │
│  通道/订单/lot/journal│
│  出款/拒付/对账/事件  │
└──────────┬───────────┘
           │ gRPC 资金变更
           ▼
┌──────────────────────┐     ┌──────────────────────┐
│  Trading Engine      │────▶│  t_account_ledger    │
│  余额/冻结/可提预检   │     │  + 账户资金快照       │
└──────────────────────┘     └──────────────────────┘
           ▲
           │ 读 entity/status/currency/can_withdraw
┌──────────┴───────────┐     ┌──────────────────────┐
│  Account / User      │     │  Partner Service     │
│  KYC等级/多账户      │     │  佣金账本独立         │
└──────────────────────┘     │  出款经 PARTNER_WALLET│
                             └──────────────────────┘
```

---

## 十四、分阶段落地与验收

### P0 — 法币可上线

**范围：** 通道、收付款方式、入金、出金、冻结、引擎 Credit/Debit/Freeze、客户 ledger、幂等、基础风控、人工审核、最小 journal、Outbox（deposit/withdrawal completed）。

**验收：**

1. 重复 PSP 回调不重复入账（幂等）  
2. Fund 进程无任何 SQL 更新 `t_trading_account.balance`  
3. 出金拒绝后余额与冻结恢复正确  
4. 入金完成后 ledger 链式连续且与引擎一致  
5. 发出 `deposit.completed.v1` / `withdrawal.completed.v1` 且可被 Inbox 消费一次  

### P1 — 合规与内部资金

**范围：** 内部转账换汇、lot FIFO 原路退回、日切对账、Chargeback、四眼审批完善、PreviewWithdraw 有持仓场景。

**验收：**

1. 本金出金强制原路；盈利部分可自选  
2. 拒付触发 `deposit.chargeback.v1`，Partner 可回拨  
3. 对账差异可定位到单笔并闭环  
4. 审批人不能等于提交人  

### P2 — 加密与监管报表

**范围：** 地址池、链上监听、批量出款、客户资金监管报表（CLIENT_MONEY_LIABILITY vs 银行/PSP 余额）。

**验收：**

1. 确认数达标才入账；重组/双花有拒绝路径  
2. 监管报表按 `entity_id` 日切可导出  

---

## 十五、表清单汇总

| # | 表名 | 说明 | 阶段 |
|---|------|------|------|
| 1 | `t_payment_channel` | 支付通道 | P0 |
| 2 | `t_user_payment_method` | 用户收付款方式 | P0 |
| 3 | `t_deposit_order` | 入金订单 | P0 |
| 4 | `t_withdrawal_order` | 出金订单 | P0 |
| 5 | `t_internal_transfer` | 内部转账 | P1 |
| 6 | `t_payout_instruction` | 统一出款指令 | P0 |
| 7 | `t_deposit_lot` | 入金批次 | P1（P0 可先写后用） |
| 8 | `t_deposit_lot_consumption` | 批次消耗 | P1 |
| 9 | `t_chargeback` | 拒付 | P1 |
| 10 | `t_account_fund_freeze` | 冻结明细 | P0 |
| 11 | `t_account_ledger` | 客户流水 | P0 |
| 12 | `t_journal_entry` / `t_journal_line` | 公司账簿 | P0 最小集 |
| 13 | `t_fund_risk_rule` / `t_fund_risk_check_log` | 资金风控 | P0 |
| 14 | `t_fund_approval` | 四眼审批 | P0/P1 |
| 15 | `t_fund_idempotency` | 幂等 | P0 |
| 16 | `t_fund_outbox` | 事件发件箱 | P0 |
| 17 | `t_exchange_rate_snapshot` | 汇率快照 | P1 |
| 18 | `t_fund_reconciliation(_detail)` | PSP 对账 | P1 |
| 19 | `t_crypto_deposit_address` | 加密地址池 | P2 |

---

## 十六、设计收口

本资金体系以三句话收口：

1. **Fund 编排支付与合规，Trading Engine 独占客户余额写入。**
2. **客户单边 ledger 保交易真相，公司复式 journal 保资金隔离与报表。**
3. **批次级原路退回 + 幂等 + Outbox + 四眼，构成可持牌运营的最小完备集。**

与账户体系的契约保持不变：单账户单币种；`frozen_balance` 快照对应 `t_account_fund_freeze` 明细；所有余额变动仍只经 `t_account_ledger`（由引擎写入）。与代理体系的契约：Fund 执行 `PARTNER_WALLET` 出款并发布 `partner.payout.*` / `deposit.chargeback.v1`，不拥有佣金账本。

---

## 十七、跨域契约对齐（v1.1 补充）

> 本节为 v1.1 对齐《平台跨域契约补充卷》v1.x 的补充章节。

### 17.1 跨域契约引用声明

- **错误码**：遵循补充卷 §三，使用 `FUND_*` 前缀。
- **事件**：遵循补充卷 §四 CloudEvents envelope。`deposit.*`/`withdrawal.*` schema 见补充卷 §4.3。
- **角色**：遵循补充卷 §五，出金操作员/审批员互斥（`PMS_FUND_PAYOUT_OPERATOR` vs `PMS_FUND_PAYOUT_APPROVER`）。
- **ID**：遵循补充卷 §六，`request_id` 由调用方生成；`reward_id`（营销发奖幂等）由营销域生成。
- **API**：对外提供 `FundService` gRPC（见补充卷 §9.1）+ Fund HTTP/gRPC API（见 §17.4）。
- **容灾/容量/监控**：遵循补充卷 §十/§十一/§十二。

### 17.2 分布式异常与补偿 Saga

旧稿对中间态异常处理不足。v1.1 定义三类高危场景的 Saga：

#### 17.2.1 CREDITING 中间态（引擎已入账，Fund 后续失败）

```
入金 PAID → 引擎 CreditAccount 成功 → Fund 写 lot/journal 失败
  状态：deposit.status = CREDITING_STUCK
  Saga：
    1. 检测：定时扫描 status=CREDITING 超过 60s
    2. 重试 Fund 写 lot/journal（幂等）
    3. 重试 N 次失败 → 标记 RECONCILE，告警人工
    4. 人工介入：核对引擎余额 vs ledger，手动补 ledger 或回滚引擎
  SLO：CREDITING_STUCK 数量 = 0（告警阈值 > 0）
```

#### 17.2.2 PAYOUT_SUBMITTED 中间态（PSP 已出款，Debit 失败）

```
出金 PAYOUT_SUBMITTED → PSP 回调成功 → Fund DebitAccount 失败
  状态：withdrawal.status = PAYOUT_DONE_DEBIT_PENDING（新增子状态）
  Saga：
    1. 锁定账户 can_withdraw=0，防止重复出金
    2. 重试 DebitAccount（幂等）
    3. 重试失败 → 标记 RECONCILE，告警 P1
    4. 人工：核对 PSP 流水 vs ledger，手动补扣或挂账待追回
  SLO：PAYOUT_DONE_DEBIT_PENDING 数量 = 0
  监控指标：`withdraw_payout_done_debit_pending`（补充卷 §12.2）
```

#### 17.2.3 PAID 后风控拒绝需原路退

```
入金 PAID → 风控/AML 拒绝 → 需原路退回 PSP
  Saga：
    1. 标记 deposit.status = REJECTED_BY_RISK
    2. 调用 PSP Refund（原路）
    3. PSP 退款成功 → ledger 写 REVERSAL
    4. 退款失败 → 人工工单，资金暂挂 SUSPENSE 科目
  SLA：拒绝后 24h 内发起退款
```

### 17.3 `margin_call_buffer` 单一真相源（解决资金域 §5.3 缺口）

旧稿可提公式引用 `margin_call_buffer` 但未定义来源。v1.1 裁定：

```
margin_call_buffer = max(
  账户组 margin_call_level 推导缓冲,   // = (1 - margin_call_level) × equity
  t_fund_risk_rule 更严值,              // 资金风控规则
  账户级 margin_call_level_override     // 账户覆盖（见账户域 §9.2）
)
```

- **生产方**：风控域在 PreTrade 前解析，产出 `margin.call.buffer.resolved.v1`（见补充卷 §4.4）。
- **消费方**：资金域出金 `PreviewWithdraw` 引用此值。
- **单一真相源**：风控域，资金域禁止自行推算。

### 17.4 Fund HTTP/gRPC API

#### gRPC（服务间，见补充卷 §9.1）

`FundService` 含 `CreditAccount`/`DebitAccount`/`FreezeFunds`/`UnfreezeFunds`/`AdjustCredit`/`PreviewWithdraw`/`TransferBetweenAccounts`/`GetAccountState`/`PayoutPartner`。

#### HTTP（对外，App/PSP Webhook）

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/deposits` | POST | 创建入金 |
| `/api/v1/withdrawals` | POST | 申请出金 |
| `/api/v1/withdrawals/preview` | POST | 出金预览（可提计算） |
| `/api/v1/transfers` | POST | 内部转账 |
| `/api/v1/callbacks/{pspCode}/deposit` | POST | PSP 入金回调 |
| `/api/v1/callbacks/{pspCode}/withdrawal` | POST | PSP 出金回调 |
| `/api/v1/callbacks/{pspCode}/chargeback` | POST | PSP 拒付通知 |

**Webhook 验签规范**：
- Header `X-PSP-Signature: <algo>=<hex>`，HMAC-SHA256(payload, shared_secret)。
- 时间戳防重放：`X-PSP-Timestamp`，±5min 窗口。
- 幂等：`X-PSP-Event-Id` 作为 `idempotency_key`。
- 验签失败返回 `FUND_EXTERNAL_PSP_WEBHOOK_INVALID`（403）。

### 17.5 `t_account_ledger` 分区与归档

旧稿 ledger 表无分区。v1.1 补：

```sql
ALTER TABLE t_account_ledger PARTITION BY RANGE (TO_DAYS(created_at)) (
  PARTITION p202607 VALUES LESS THAN (TO_DAYS('2026-08-01')),
  PARTITION p202608 VALUES LESS THAN (TO_DAYS('2026-09-01')),
  ...
  PARTITION pmax VALUES LESS THAN MAXVALUE
);
```

- 月度 RANGE 分区，自动新增。
- 冷归档：> 2 年数据迁移至冷存储（S3 + 索引），保留审计查询能力。
- 覆盖索引：`(account_id, id)` 支持按账户顺查。

### 17.6 三方对账（引擎余额 vs ledger vs 账户快照）

旧稿 §12.2 仅 4 条审计 SQL。v1.1 增第 5 条日切作业：

```
日切对账（T+1 08:00 前）：
  1. 引擎内存余额快照（dump 至 t_account_snapshot_eod）
  2. ledger Σ balance_after 按 account_id 聚合
  3. 账户快照 t_trading_account.balance
  4. 三方比对：
     - 引擎 vs ledger：不一致 → 自动修复（以 ledger 为准，引擎重放）
     - ledger vs 账户快照：不一致 → 告警人工（账户快照为投影，可能 lag）
  5. 阈值：自动修复 < 1 USD，超阈值人工工单
  监控：`account_ledger_mismatch`（补充卷 §12.2）
```

### 17.7 SLO

| 指标 | SLO |
|------|-----|
| 入金 PAID → COMPLETED P99 | < 30s |
| 出金 FROZEN → COMPLETED P99 | < 4h |
| 对账作业完成 | T+1 08:00 前 |
| `CREDITING_STUCK` 数量 | 0 |
| `PAYOUT_DONE_DEBIT_PENDING` 数量 | 0 |
| Webhook 验签失败率 | < 0.1% |

### 17.8 AML 深度集成

旧稿仅消费 KYC 结果。v1.1 扩展：

- **快进快出检测**：入金后 24h 内出金 > 80% 且无交易 → 标记 `t_risk_suspicious_flag`，转人工。
- **Structuring 检测**：单日多笔入金恰低于申报阈值 → 标记。
- **AML 命中后**：`ONB_STATE_AML_SANCTION_HIT` → 冻结账户 `can_withdraw=0`，发 `user.frozen.v1`。
- **SAR/STR 报送**：合规官 `PMS_COMPLIANCE_OFFICER` 经审批后报送监管，留痕 `t_fund_compliance_log`。

### 17.9 错误码（引用补充卷 §三）

| code | 含义 | retryable |
|------|------|-----------|
| `FUND_LIMIT_INSUFFICIENT_BALANCE` | 余额不足 | false |
| `FUND_LIMIT_WITHDRAW_EXCEEDS_AVAILABLE` | 出金超可提 | false |
| `FUND_LIMIT_FROZEN_BY_RISK` | 风控冻结 | false |
| `FUND_STATE_DEPOSIT_STUCK_CREDITING` | 入金卡 CREDITING | true |
| `FUND_STATE_WITHDRAW_PAYOUT_DONE_DEBIT_PENDING` | 钱已出账未扣 | true |
| `FUND_EXTERNAL_PSP_TIMEOUT` | PSP 超时 | true |
| `FUND_EXTERNAL_PSP_WEBHOOK_INVALID` | Webhook 验签失败 | false |
| `FUND_CONFLICT_DUPLICATE_REQUEST` | 重复请求 | false |

### 17.10 修订历史

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 Final | 2026-07 前 | 终版（旧） |
| v1.1 | 2026-07-21 | 对齐补充卷：Saga 补偿三场景、margin_call_buffer 单一真相源、Fund HTTP API + Webhook 验签、ledger 分区归档、三方对账、SLO、AML 深度集成、错误码 |
